# 🤖 NLBW VPN & Telegram Bot 全栈部署系统 

这是一个基于 Python Pyrogram 的服务器自动化管理系统。
集成 Xray 节点管理、系统监控、自动防火墙、WARP 流媒体解锁及每日战报推送。

## ✨ 核心功能 (v4.0 Ultra)
- **全能网络**: VLESS + Socks5 双协议，支持 WARP 自动解锁 Netflix/Disney+
- **安全基线**: 自动配置防火墙 (UFW/Firewalld/Iptables)，仅开放必要端口
- **系统优化**: 智能 Swap 内存管理 + BBR 内核加速
- **交互管理**: Telegram 机器人一键增删节点、查看日志、获取二维码
- **无人值守**: 每日 08:00 自动发送流量/性能战报

## 🚀 快速部署

**系统要求**: Debian 10+ / Ubuntu 20.04+ / CentOS 7+ (推荐 Debian 11/12)

在 VPS 上以 Root 身份执行以下命令：

### 1. 安装 Git（如果系统未安装）
```bash
# 自动检测并安装 git
if ! command -v git >/dev/null 2>&1; then
echo "[+] Git 未安装，正在自动安装..."
if command -v apt >/dev/null 2>&1; then
apt update && apt install -y git
elif command -v yum >/dev/null 2>&1; then
yum install -y git
else
echo "[-] 不支持的系统，无法自动安装 Git"
exit 1
fi
fi
```
### 2. 拉取代码
```bash
git clone [https://github.com/hupan0210/nlbw-bot-project.git](https://github.com/hupan0210/nlbw-bot-project.git)
cd nlbw-bot-project
```
### 3. 授权并安装
```bash
chmod +x install.sh
./install.sh
```

## 📂 目录结构 
**安装完成后，程序位于**/opt/nlbw/：

- **tgbot/main.py**: 机器人核心源码

- **tgbot/config.json**: 配置文件 (包含 Token)

- **tgbot/venv/**: Python 虚拟环境

- **scripts/daily_report.sh**: 每日战报脚本

## 🛠️ 常用维护命令
- **重启机器人**: systemctl restart nlbw_bot

- **查看运行日志**: journalctl -u nlbw_bot -f

- **停止服务**: systemctl stop nlbw_bot

- **手动发送战报**: bash /opt/nlbw/scripts/daily_report.sh

## Generated by NLBW Project
